package com.majortom.algorithms.visualization.impl.controller;

/** Selectable runtime element type and availability in the current module. */
record ValueTypeOption(String type, boolean available) {}
