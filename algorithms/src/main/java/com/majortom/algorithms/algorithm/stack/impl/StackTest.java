package com.majortom.algorithms.algorithm.stack.impl;

import com.majortom.algorithms.algorithm.stack.StackAlgorithm;
import com.majortom.algorithms.core.annotation.Algorithm;
import com.majortom.algorithms.structure.linked.StackStructure;


@Algorithm(id = "stack-test", name = "栈测试", module = "stack", type = Integer.class, structure = StackStructure.class)
public class StackTest implements StackAlgorithm<Integer> {

    @Override
    public void execute(StackStructure<Integer> stack) {

    }

}
